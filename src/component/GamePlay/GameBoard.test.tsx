import { create, act, ReactTestRenderer } from "react-test-renderer";
import { GameBoard } from "./GameBoard";

describe("GameBoard tests", () => {
  test("When component loads should", () => {
  });
})